require('dotenv').config();

module.exports = {
    token: process.env.TOKEN,
    prefix: process.env.PREFIX
}

/*
 ISI TOKEN DAN PREFIX MU DI .ENV ATAU OPSI DIATAS BISA DIUBAH JADI:
 
 token: "Token Bot Mu",
 prefix: "Prefix Bot Mu"
*/